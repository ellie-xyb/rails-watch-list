<div class="list-banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= @list.bg_url %>);">
  <div class="container">
    <h1 class="mb-3"><%= @list.name.capitalize %></h1>
    <%= link_to "Lists<<", lists_path, class: "btn btn-outline-warning mr-2" %>
    <%= link_to "➕ Movie",  new_list_bookmark_path(@list), class: "btn btn-info" %>
  </div>
</div>
<%= render "shared/movie-cards" %>

<!-- app/views/articles/index.html.erb -->
<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<!-- for face detection -->
<%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>

